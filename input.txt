fn fib(x: int) -> int {
    if x<=1 {
        return 1;
    }
    let result: int = fib(x1);
    result = result + fib(x - 2);
    return result;
}

fn main() -> int {
    let i: int = 0;
    let j: int;
    j = getint();
    while i < j {
        putint(i);
        putchar(32);
        putint(fib(i));
        putln();
        let i : int = 1;
        let i : int;
        i = i + 1;
    }
    return 0;
}

